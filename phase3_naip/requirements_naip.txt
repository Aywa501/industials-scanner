# Phase 3 NAIP worker — SAM 3 + NAIP COG reads + telemetry.
#
# AUDIT (2026-05-22): the [[pin_transformers]] memory estimated SAM 3 in ≥4.57;
# verified empirically that `Sam3Model` first ships in transformers 5.0.0 — not
# in any 4.x release. The wheel inspection (transformers 4.57.0 → 5.9.0) found
# `transformers/models/sam3/` only from 5.0.0 onward. Pin to the latest 5.x
# patch with the same API surface we coded against. v2's 4.56.0 pin (for
# PT 2.4.1) does NOT include SAM 3; DLAMI Nov-2025+ ships PT 2.6, which is
# compatible with transformers 5.x.
transformers==5.9.0
rasterio>=1.3.9
boto3>=1.34.0
pyproj>=3.6.0
pandas>=2.1.0
pyarrow>=14.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
shapely>=2.0.0
huggingface_hub>=0.19.0
# psutil drives the SystemSampler (CPU/RAM/disk/net telemetry). nvidia-smi is
# shelled out, so no python GPU dep needed.
psutil>=5.9.0
