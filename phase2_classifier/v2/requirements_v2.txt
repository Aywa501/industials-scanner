# Pinned versions known-good for v2 multi-model embedding run on g6.2xlarge DLAMI.
# Replays the env we landed on after working through the terratorch/timm/transformers
# version dance. Install order matters because terratorch/timm pull torch deps.
#
# Use:
#   pip install --no-deps -r requirements_v2.txt          # in /opt/pytorch venv
#   # or, if torch needs explicit cu128 wheel:
#   pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 --extra-index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements_v2.txt

# torch + torchvision must come from the cu128 wheel index (commented above);
# pinning here so a re-install on the same box matches what was working.
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# HF stack: 4.56 is the floor that recognizes dinov3_vit. terratorch needs timm.
transformers==4.56.0
timm==1.0.26
huggingface-hub
safetensors==0.8.0rc0

# Prithvi-EO-2.0 backbone registry. Side-effect import registers backbones.
terratorch==1.2.7
einops==0.7.0

# Geo + IO
rasterio==1.5.0
pystac-client==0.9.0
pyproj==3.7.2
mgrs==1.5.4

# Data + AWS
pandas==3.0.2
pyarrow==24.0.0
numpy==2.4.4
boto3==1.43.5
pillow==12.2.0
scikit-learn==1.8.0
python-dotenv==1.2.2
